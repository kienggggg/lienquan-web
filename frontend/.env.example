# Sao chép thành .env rồi điền. Cả 2 biến này PHẢI đặt trên Vercel (Settings > Environment Variables) khi deploy.

# Chuỗi bí mật ký JWT — tạo mạnh: openssl rand -base64 48
JWT_SECRET=

# Postgres đám mây (Neon/Vercel Postgres). Dạng: postgresql://user:pass@host/db?sslmode=require
DATABASE_URL=
